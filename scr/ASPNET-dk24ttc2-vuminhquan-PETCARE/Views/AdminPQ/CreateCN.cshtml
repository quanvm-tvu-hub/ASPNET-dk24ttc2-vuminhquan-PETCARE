@model WebsiteThuCungBento.Models.CHUCNANG_QUYEN

@{
    ViewBag.Title = "Thêm Chức Năng Mới";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card border-0 shadow-lg rounded-4 overflow-hidden">
                <div class="card-header bg-primary text-white p-4 border-0">
                    <div class="d-flex align-items-center">
                        <div class="bg-white bg-opacity-25 rounded-circle p-3 me-3">
                            <i class="fa-solid fa-plus-circle fa-2x"></i>
                        </div>
                        <div>
                            <h3 class="fw-bold mb-1">Thêm Chức Năng Mới</h3>
                            <p class="mb-0 opacity-75">Tạo chức năng phân quyền mới cho hệ thống</p>
                        </div>
                    </div>
                </div>

                <div class="card-body p-5">
                    @using (Html.BeginForm("CreateCN", "AdminPQ", FormMethod.Post, new { @class = "needs-validation" }))
                    {
                        @Html.AntiForgeryToken()
                        
                        if (!ViewData.ModelState.IsValid)
                        {
                            <div class="alert alert-danger alert-dismissible fade show mb-4" role="alert">
                                <i class="fa-solid fa-triangle-exclamation me-2"></i>
                                Vui lòng kiểm tra lại thông tin nhập vào.
                                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                            </div>
                        }

                        <div class="mb-4">
                            <label class="form-label fw-medium text-secondary">Mã Chức Năng <span class="text-danger">*</span></label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-end-0"><i class="fa-solid fa-key text-muted"></i></span>
                                @Html.TextBoxFor(model => model.MACHUCNANG, new { @class = "form-control border-start-0 ps-0", @placeholder = "Ví dụ: QL_SANPHAM", @required = "required" })
                            </div>
                            @Html.ValidationMessageFor(model => model.MACHUCNANG, "", new { @class = "text-danger small mt-1" })
                            <div class="form-text small">Mã chức năng nên viết hoa và không dấu (VD: QL_USER)</div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label fw-medium text-secondary">Tên Chức Năng <span class="text-danger">*</span></label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-end-0"><i class="fa-solid fa-tag text-muted"></i></span>
                                @Html.TextBoxFor(model => model.TENCN, new { @class = "form-control border-start-0 ps-0", @placeholder = "Ví dụ: Quản lý sản phẩm", @required = "required" })
                            </div>
                            @Html.ValidationMessageFor(model => model.TENCN, "", new { @class = "text-danger small mt-1" })
                        </div>

                        <div class="d-flex justify-content-end gap-2 mt-5 pt-3 border-top">
                            <a href="@Url.Action("DSChucNang", "AdminPQ")" class="btn btn-light px-4 rounded-pill fw-medium">
                                <i class="fa-solid fa-arrow-left me-2"></i>Quay lại
                            </a>
                            <button type="submit" class="btn btn-primary px-4 rounded-pill fw-bold shadow-sm hover-scale">
                                <i class="fa-solid fa-plus me-2"></i>Tạo mới
                            </button>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .form-control:focus {
        border-color: #0d6efd;
        box-shadow: none;
        border-left: 1px solid #0d6efd !important;
    }
    
    .input-group:focus-within .input-group-text {
        border-color: #0d6efd;
    }
    
    .input-group:focus-within .form-control {
        border-left: none !important;
    }

    .hover-scale {
        transition: transform 0.2s;
    }
    
    .hover-scale:hover {
        transform: translateY(-2px);
    }
</style>
