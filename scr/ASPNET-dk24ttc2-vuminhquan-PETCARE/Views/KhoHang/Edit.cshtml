@model WebsiteThuCungBento.Models.PHIEUNHAPKHO

@{
    ViewBag.Title = "Cập Nhật Phiếu Nhập Kho";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card border-0 shadow-lg rounded-4 overflow-hidden">
                <div class="card-header bg-warning text-dark p-4 border-0">
                    <div class="d-flex align-items-center">
                        <div class="bg-white bg-opacity-50 rounded-circle p-3 me-3">
                            <i class="fa-solid fa-warehouse fa-2x"></i>
                        </div>
                        <div>
                            <h3 class="fw-bold mb-1">Cập Nhật Phiếu Nhập</h3>
                            <p class="mb-0 opacity-75">Chỉnh sửa thông tin phiếu nhập kho #@Model.MAPHIEUNK</p>
                        </div>
                    </div>
                </div>

                <div class="card-body p-5">
                    @using (Html.BeginForm("Edit", "KhoHang", FormMethod.Post, new { @class = "needs-validation" }))
                    {
                        @Html.AntiForgeryToken()
                        @Html.HiddenFor(model => model.MAPHIEUNK)

                        if (!ViewData.ModelState.IsValid)
                        {
                            <div class="alert alert-danger alert-dismissible fade show mb-4" role="alert">
                                <i class="fa-solid fa-triangle-exclamation me-2"></i>
                                Vui lòng kiểm tra lại thông tin nhập vào.
                                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                            </div>
                        }

                        <div class="mb-4">
                            <label class="form-label fw-medium text-secondary">Ngày Nhập <span class="text-danger">*</span></label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-end-0"><i class="fa-solid fa-calendar text-muted"></i></span>
                                @Html.EditorFor(model => model.NGAYNK, new { htmlAttributes = new { @class = "form-control border-start-0 ps-0", @type = "datetime-local", @required = "required" } })
                            </div>
                            @Html.ValidationMessageFor(model => model.NGAYNK, "", new { @class = "text-danger small mt-1" })
                        </div>

                        <div class="mb-4">
                            <label class="form-label fw-medium text-secondary">Sản Phẩm <span class="text-danger">*</span></label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-end-0"><i class="fa-solid fa-box text-muted"></i></span>
                                @Html.DropDownList("MASP", null, new { @class = "form-select border-start-0 ps-0" })
                            </div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label fw-medium text-secondary">Số Lượng <span class="text-danger">*</span></label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-end-0"><i class="fa-solid fa-arrow-up-9-1 text-muted"></i></span>
                                @Html.EditorFor(model => model.SOLUONG, new { htmlAttributes = new { @class = "form-control border-start-0 ps-0", @type = "number", @min = "1", @placeholder = "Nhập số lượng", @required = "required" } })
                            </div>
                            @Html.ValidationMessageFor(model => model.SOLUONG, "", new { @class = "text-danger small mt-1" })
                        </div>

                        <div class="d-flex justify-content-end gap-2 mt-5 pt-3 border-top">
                            <a href="@Url.Action("Index", "KhoHang")" class="btn btn-light px-4 rounded-pill fw-medium">
                                <i class="fa-solid fa-arrow-left me-2"></i>Quay lại
                            </a>
                            <button type="submit" class="btn btn-warning px-4 rounded-pill fw-bold shadow-sm hover-scale">
                                <i class="fa-solid fa-save me-2"></i>Cập nhật
                            </button>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .form-control:focus, .form-select:focus {
        border-color: #ffc107;
        box-shadow: none;
        border-left: 1px solid #ffc107 !important;
    }
    
    .input-group:focus-within .input-group-text {
        border-color: #ffc107;
    }
    
    .input-group:focus-within .form-control, 
    .input-group:focus-within .form-select {
        border-left: none !important;
    }

    .hover-scale {
        transition: transform 0.2s;
    }
    
    .hover-scale:hover {
        transform: translateY(-2px);
    }
</style>
